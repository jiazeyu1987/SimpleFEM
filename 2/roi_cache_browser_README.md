# ROI Analysis Cache Browser - 使用说明

`roi_cache_browser.py` 是一个独立的 GUI 应用程序，用于浏览和对比 SimpleFEM 生成的 `roi_analysis_cache_*.jsonl` 文件。

## 快速开始

### 运行程序

```bash
python roi_cache_browser.py
```

或者在 Windows 上直接双击 `roi_cache_browser.py` 文件。

### 加载数据

1. 点击工具栏的 **"选择文件"** 按钮
2. 选择一个 `roi_analysis_cache_*.jsonl` 文件
3. 程序会自动加载并显示第一帧数据

## 功能说明

### 1. 单帧浏览模式

**功能**: 查看每一帧的完整数据，支持快速导航

**使用方法**:
- 使用 **导航按钮** 浏览帧：
  - `|<` 首帧
  - `<` 前一帧
  - `>` 后一帧
  - `>|` 末帧
- 在输入框中输入帧号，按回车或点击 **"跳转"** 按钮
- 使用 **"上一峰"** / **"下一峰"** 快速跳转到波峰帧

**数据分组**:
- **基本信息**: 帧索引、时间、视频时间等
- **ROI 区域**: ROI1 坐标、绿线交点、ROI2/ROI3 数据
- **检测参数**: 缓冲区状态、阈值、检测参数
- **波峰检测**: 绿色/红色波峰数量

**字段说明**:
- 鼠标悬停在字段名上，会显示该字段的详细说明
- 例如：悬停在 "roi2_gray" 上，会显示 "ROI2区域的平均灰度值 (0-255)"

### 2. 双帧对比模式

**功能**: 并排显示两帧数据，自动高亮差异

**使用方法**:
1. 切换到 **"双帧对比"** 标签页
2. 选择要对比的两个帧：
   - 在输入框中手动输入帧 A 和帧 B 的索引
   - 点击 **"对比相邻帧"** 快速对比当前帧和下一帧
   - 点击 **"对比波峰帧"** 对比前两个检测到的波峰帧
3. 点击 **"开始对比"** 按钮

**差异高亮**:
- 数值差异超过 **5%** 的字段会被高亮显示
- **绿色背景**: 值增加（例如：roi2_gray 从 100 增加到 120）
- **红色背景**: 值减少（例如：roi2_gray 从 100 减少到 80）
- **箭头标记**: 帧 A 显示 "→"，帧 B 显示 "→ 值 (±X%)"

**示例**:
```
帧 A                          帧 B
roi2_gray: 100.00 →         roi2_gray: → 120.00 (+20.0%)
video_seconds: 10.00 →       video_seconds: → 10.07 (+0.7%)
```

### 3. 波形趋势图模式

**功能**: 显示 ROI2 灰度值随帧变化的曲线，标记峰值位置

**使用方法**:
1. 切换到 **"波形趋势"** 标签页
2. 图表会自动显示所有帧的数据
3. **点击图表** 上的任意位置，跳转到对应的帧

**图表元素**:
- **绿色曲线**: ROI2 灰度值
- **红色虚线**: 检测阈值
- **绿色圆点**: 绿色波峰位置
- **红色圆点**: 红色波峰位置
- **蓝色虚线**: 当前浏览帧的位置指示

**交互功能**:
- 点击图表跳转到对应帧
- 查看整体趋势和波峰分布
- 识别波峰的模式和周期

## 快捷操作

### 键盘快捷键

在单帧浏览模式下：
- **回车键**: 跳转到输入框中指定的帧
- 在帧输入框中按回车直接跳转

### 快速导航按钮

- **上一峰**: 跳转到上一个绿色波峰帧
- **下一峰**: 跳转到下一个绿色波峰帧

## 数据字段说明

### 基本信息

| 字段 | 说明 |
|------|------|
| frame_index | 帧的序列索引号，从1开始计数 |
| ts_local | 本地时间戳，ISO 8601 格式 |
| video_seconds | 视频播放时间（秒） |
| screen_size | 屏幕/视频分辨率 [宽度, 高度] |

### ROI 区域

| 字段 | 说明 |
|------|------|
| roi1 | ROI1 区域坐标 {x1, y1, x2, y2}，大区域用于绿线检测 |
| intersection.used | 实际使用的绿线交点坐标（经过滤波处理） |
| roi2_region | ROI2 区域坐标 [x1, y1, x2, y2] |
| roi2_gray | ROI2 区域的平均灰度值 (0-255)，用于波峰检测 |
| roi3.gray | ROI3 区域的平均灰度值 |

### 检测参数

| 字段 | 说明 |
|------|------|
| buffer.len | 缓冲区当前长度 |
| threshold.used | 实际使用的阈值（固定或自适应） |
| threshold.adaptive_enabled | 是否启用自适应阈值 |
| threshold.protection_active | 阈值保护是否激活（防止波峰污染背景） |
| detect_params.difference_threshold | 绿/红判定阈值 |

### 波峰检测

| 字段 | 说明 |
|------|------|
| peaks.green | 检测到的绿色波峰列表（经过去重处理） |
| peaks.red | 检测到的红色波峰列表（经过去重处理） |

## 常见使用场景

### 场景1: 查看特定帧的详细数据

1. 打开 JSONL 文件
2. 在单帧浏览模式下，输入帧索引或使用导航按钮
3. 鼠标悬停在字段名上查看说明

### 场景2: 对比波峰帧的数据

1. 切换到双帧对比模式
2. 点击 **"对比波峰帧"** 按钮
3. 查看差异高亮，分析波峰前后数据变化

### 场景3: 分析整体波形趋势

1. 切换到波形趋势图模式
2. 观察波峰的分布和周期
3. 点击感兴趣的波峰，跳转到对应帧查看详情

### 场景4: 追踪交点位置变化

1. 在单帧浏览模式下，使用导航按钮逐帧查看
2. 观察 `intersection.used` 字段的变化
3. 切换到对比模式，对比不同帧的交点位置

## 技术信息

### 依赖项

程序需要以下 Python 库：
- `tkinter` (GUI 框架，Python 内置)
- `numpy` (数值计算)
- `matplotlib` (波形图绘制)

安装依赖：
```bash
pip install numpy matplotlib
```

### 支持的文件格式

- 文件类型: JSONL (JSON Lines)
- 文件命名: `roi_analysis_cache_*.jsonl`
- 编码: UTF-8

### 性能说明

- 支持大文件（数万帧）
- 内存占用: 约为文件大小的 2-3 倍
- 加载速度: 约 1000 帧/秒

## 故障排除

### 问题1: 程序无法启动

**解决方案**:
- 确认已安装 Python 3.8+
- 安装依赖: `pip install numpy matplotlib`

### 问题2: 文件加载失败

**解决方案**:
- 确认文件格式为 JSONL
- 确认文件编码为 UTF-8
- 检查文件是否损坏

### 问题3: 中文字符显示异常

**解决方案**:
- 确认系统安装了中文字体（SimHei 或 Microsoft YaHei）
- 如果使用 Linux，安装中文字体包

### 问题4: 波形图不显示

**解决方案**:
- 确认已成功加载文件
- 切换到其他标签页再切换回来
- 检查数据是否包含 roi2_gray 字段

## 扩展功能

### 未来计划

- [ ] 导出当前帧数据为 CSV
- [ ] 导出波形图为图片
- [ ] 支持多文件同时打开
- [ ] 添加数据筛选功能（按阈值、波峰数量等）
- [ ] 支持自定义字段说明
- [ ] 添加统计报表

## 反馈和支持

如有问题或建议，请联系 SimpleFEM 开发团队或提交 Issue。
